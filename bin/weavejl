#!/bin/bash


# weave julia markdown files
if [ -z "$1" ]
then
  echo -e "No Julia Markdown doc passed - exiting."
  exit
fi

julia -O0 -e "
  using Weave;
  weave(normpath(@__DIR__, \"$1\"); doctype = \"md2html\")
"
